setup_complete = true

[database]
database_url = "postgres://rustpress:rustpress@localhost:5433/rustpress"

[server]
host = "127.0.0.1"
port = 8089

[auth]
# Placeholder only. Set JWT_SECRET in the environment for any non-dev run;
# the server loads env first and falls back to this value with a warning.
# The previous literal secret was committed by accident — treat any token
# signed against it as compromised and rotate.
jwt_secret = "change-me-in-production"
